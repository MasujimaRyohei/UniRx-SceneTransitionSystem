namespace UniRxSceneTransition
{
    /// <summary>
    /// Scenes config.
    /// You have to put them together in order of build setting.
    /// </summary>
    public enum EScenes : int
    {
        Prepare = 0,
        Logo,
        Title,
        TitleUI,
        Main,
        MainUI,
        MainPause
    }
}